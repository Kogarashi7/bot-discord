module.exports = {
    name: 'xd', // Nombre del comando
    description: 'x ', // Descripción del comando
    category: 'x ', // Categoría al que se va adjuntar en el comando
    execute(client, message, args) {
      message.reply('Arroz con pollo'); // Respuesta al comando
    }
  };
  